# M4 Optional Phase — Deliberately Not Planned Yet

**Status:** stub, by design (YAGNI). PRD §13 gates M4 on "only if M2/M3 economics justify it" — writing detailed plans for work that may never happen is waste.

## Trigger conditions → what to plan then

| Item | Plan it when |
|---|---|
| Multi-leader support | A second leader worth copying is actually identified |
| Remote dashboard access (phone) | The localhost-only dashboard has concretely blocked a decision (needs auth design first — PRD §15 Q2) |
| Playback / replay on the chart | Someone has actually needed to replay history beyond what SQLite queries answer |
| EMA/SMA/Bollinger indicators | Never, probably — not needed to supervise a copybot |

When a trigger fires, write the plan with the same superpowers:writing-plans process used for M1–M3.
