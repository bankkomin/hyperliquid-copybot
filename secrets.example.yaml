# Copy to secrets.yaml (git-ignored) ONLY when going live.
# Use a Hyperliquid API wallet (agent) key — never your main wallet key.
# Create one at app.hyperliquid.xyz -> More -> API.
api_wallet_key: "0x..."
